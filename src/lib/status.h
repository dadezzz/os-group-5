#ifndef STATUS_H
#define STATUS_H

// We can add more error types here
typedef enum Errors {
  STATUS_OK, STATUS_INVALID_ORDER, STATUS_RESOURCE_UNAVAILABLE, STATUS_INVALID_PARAMETER, STATUS_FILE_NOT_FOUND
};

#endif
