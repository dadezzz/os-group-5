# OS Project (Group 5)

## Getting started

Install `clang`, `clang-tools-extra` (for `clang-tidy`), `npm`

Compile with:

```bash
make
```

For compiling with LSP integration run:

```bash
CC=clang make
```

## Contributing

Code must be formatted with:

```bash
make format
```
